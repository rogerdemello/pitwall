# The main requirements.txt minus the data-pipeline half: this Space serves one
# clip at a time and never builds a corpus, so fastf1 / datasets / duckdb / jiwer
# are all dead weight in the image.
#
# torch is NOT pinned to the CPU wheel here, unlike the root requirements.txt.
# ZeroGPU needs the CUDA build, and its runtime requires torch >= 2.8.

torch==2.9.1
transformers==5.15.0
librosa==0.11.0
soundfile==0.14.0
numpy==1.26.2
scipy==1.16.3
scikit-learn==1.8.0

# Not for transcription here - the Space runs Whisper through transformers. This
# is for the Silero VAD it bundles as ONNX, which prosody needs to window over
# speech instead of over silence and channel noise.
faster-whisper==1.2.1

gradio==5.49.1
spaces==0.42.1
